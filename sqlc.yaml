version: "2"
sql:
  - engine: "mysql"
    queries: "internal/store/mysqlstore/queries.sql"
    schema: "internal/store/mysqlstore/migrations"
    gen:
      go:
        package: "mysqlstore"
        out: "store/mysqlstore"
        emit_interface: false
        emit_json_tags: true
